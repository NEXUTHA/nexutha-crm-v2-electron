# NEXUTHA CRM 修理台帳 2026-07

**作成**: 2026-07-20 / **対象**: NEXUTHA-CRM-V2-electron（HEAD `ba64b3b`）
**運用ルール**: この台帳に無い修正は行わない。発見 → 台帳追加 → 報告 → 承認 → 修正 → 状態更新。

**状態**: 未着手 / 修正済 / 検証済 / 対応済（=リリース済） / 保留

---

## 🔴 公開ブロッカー一覧（これが残っている限り出荷不可）

| ID | 症状 | 状態 |
|---|---|---|
| **L-01** | ライセンス認証サーバー（Supabase）が消滅。**全ユーザーが起動できない** | 未着手（Stage 2で対応） |
| **N-01** | 主要ライブラリをCDNから実行時取得。**オフラインだとアプリ全損**（白画面） | 未着手 |
| ~~W-01~~ | 起動後の白画面（onefile起動遅延） | **対応済**（c631337/692bb1e/13f07e9） |
| ~~B-01~~ | CIがタグpushで即公開リリースを作成 | **対応済**（ba64b3b） |

### v2.5.2 再カット最小セット案

**含める**（ブロッカーのみ）:
1. `W-01` 白画面3コミット — **すでにHEADに入っている**
2. `B-01` ドラフト作成 — **すでにHEADに入っている**
3. **`L-01`** ライセンスのオフライン署名検証化（Stage 2）
4. **`N-01`** CDN 4ライブラリのローカル同梱

**含めない**: それ以外すべて。機能追加・リファクタリング・セキュリティ強化（S-01等）は v2.5.3 へ。

> **判断根拠**: `N-01` を含めない選択肢もあるが、`L-01` をオフライン化しても `N-01` が残ると「ネットが無いと起動できない」状態は解消せず、**オフライン対応という修正の目的が達成されない**。両方セットで初めて意味を持つ。

---

## 1. ライセンス / 起動（L）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| **L-01** | **ライセンス認証が誰も通らない**。正しいキーでも弾かれる | `index.html:6103` が `gvaoljanjtgotzjzcfta.supabase.co` に問い合わせるが**NXDOMAIN（プロジェクト消滅）**。`:6111-6112` の `catch` が無条件 `return false`、`:6370-6373` で起動処理ごと `return` | **公開ブロッカー** | 大 | **v2.5.2再カット** | 未着手 |
| L-02 | オフライン猶予が無い（fail-closed） | 同上。ネット断＝即ブロック | 公開ブロッカー（L-01に包含） | — | v2.5.2再カット | 未着手 |
| L-03 | 旧オフライン方式のデッドコードが残存 | `index.html:6083` `_LK` / `:6085-6090` `_dk()`。`verifyLicenseKey` から呼ばれていない。**旧アルゴリズムの鍵が配布物に残っている** | 中 | 小 | v2.5.2再カット | 未着手 |
| L-04 | 検証結果に署名が無く、応答偽装で回避可能 | `index.html:6109-6110` `data.valid === true` を信じるだけ | 高 | 中 | v2.5.3 | 未着手 |
| L-05 | 起動時1回のみ検証。期限切れ・解約の途中失効に対応できない | `index.html:6368` | 中 | 中 | v2.6 | 未着手 |
| L-06 | DevToolsで認証を回避できる | `main.js:602-604` が配布版でも `Cmd+Alt+I` を許可 | 中 | 小 | v2.5.3 | 未着手 |
| L-07 | ライセンス検証の自動テストが存在しない | `tests/01_license.test.js` は旧V2にしかなく、内容もダッシュボード表示確認のみ | 高 | 中 | v2.5.3 | 未着手 |

---

## 2. 外部依存 / オフライン（N）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| **N-01** | **オフラインだとアプリが完全に死ぬ**（白画面・操作不能） | `index.html:30-33` が Dexie/jsPDF/autotable/html2canvas を **cdnjs から実行時取得**。配布物にローカルコピー **0件**（実測）。`index.html:2044` の `const db = new Dexie(...)` は**トップレベル無条件実行**のため、失敗すると `1931-6734` の全4,804行が未定義になる | **公開ブロッカー** | 中 | **v2.5.2再カット** | 未着手 |
| N-02 | 更新直後の初回起動がオフラインだと N-01 が確実に発火 | `main.js:45-62` `clearHttpCacheOnVersionChange()` が版変更のたびにHTTPキャッシュを消すため、CDNの残骸も消える | 公開ブロッカー（N-01に包含） | — | v2.5.2再カット | 未着手 |
| N-03 | **使っていないライブラリの読み込み失敗でアプリが落ちる** | `USE_BACKEND=true`（`index.html:5806`）のため Dexie は実質未使用。`db.` の実利用は `!USE_BACKEND` ガード内のみ | 高 | 小 | v2.5.2再カット（N-01と同時） | 未着手 |
| N-04 | オフラインで顧客登録ウィザードが住所入力から進めない（無限ループ） | `index.html:4029-4037` `lookupZip` が失敗 → `:4048-4051` が `askZip()` に戻す。**手入力へのスキップ導線が無い** | 中 | 小 | v2.5.3 | 未着手 |
| N-05 | Google Fonts 不達でPDFの日本語フォントが変わる | `index.html:34` / `js/pdf.js:246` | 低 | 小 | v2.6 | 未着手 |
| N-06 | CSPにデッドエントリ | `index.html:27` の `raw.githubusercontent.com` は `fetch` 0件。開発ポート `localhost:3456` も配布版CSPに残存 | 低 | 小 | v2.5.3 | 未着手 |

**fail-open（死んでも実害小）**: `api.github.com`／GitHub Releases（更新通知のみ・`main.js:545-547` でログのみ）、`huggingface.co`（AI機能のみ）、`fonts.*`（見た目のみ）、`nexutha.net`（リンクのみ）
**Python backend の外部通信は 0 件**（`backend/app.py` に `requests`/`httpx`/`urlopen` なし、配布バイナリにも通信ライブラリ非同梱）— **完全ローカル**

---

## 3. セキュリティ（S）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| **S-01** | **同一LANの誰でも全顧客データを読み書き・削除できる** | `backend/app.py:1294` が `host="0.0.0.0"`、`:51-57` が `allow_origins=["*"]`＋`allow_credentials=True`、**全48APIに認証なし**。`/api/backup/export` は全顧客・全書類を1リクエストで返す | 高 | 小 | v2.5.3 | 未着手 |
| S-02 | 悪意あるWebページのJSからローカルAPIを叩ける | CORS が要求元オリジンを反射（Starlette実装で確認済み） | 高 | 小 | v2.5.3 | 未着手 |
| S-03 | XSS対策関数が実質機能していない | `js/renderer.js:808-811` `safeHtml` はコメントに反し `innerHTML` 直代入。`safeText`（`index.html:1996`）の使用は2箇所のみで、顧客名・品目名は直接埋め込み | 中 | 中 | v2.5.3 | 未着手 |
| S-04 | 旧V2リポジトリのgit履歴に顧客DB（4.4MB）が永久残存 | `7ab9d14` で追加、`45ed0cc` で除外。**履歴からは消えていない**。remote は private だが blob は取得可能 | 高 | 中 | 別途（Part 6） | 未着手 |
| S-05 | `.venv` 2,212ファイルがgit追跡（旧V2） | `.gitignore` に `venv` の記述なし | 中 | 小 | 別途（Part 6） | 未着手 |

---

## 4. データ層（D）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| D-01 | 顧客を削除しても取引・ファイル・名刺・書類履歴が孤児として残る | `backend/app.py:413-420` が customers/documents のみ削除。FK宣言はあるが `PRAGMA foreign_keys` を**一度も ON にしていない**ため無効 | 高 | 中 | v2.5.3 | 未着手 |
| D-02 | 書類更新で「0」「空」に修正できない | `app.py:1018-1041` の `doc.X or ex['X']` パターン。`0`・空文字が既存値に戻る。特に `:1038` discount、`:1040-1041` tax10/tax8 | 高 | 小 | v2.5.3 | 未着手 |
| D-03 | 書類更新で銀行情報が消える | `app.py:1029` のみフォールバック無しで `json.dumps(doc.bank_info or {})` を無条件上書き | 高 | 小 | v2.5.3 | 未着手 |
| D-04 | 書類番号が重複・欠番しうる。年をまたいでもリセットされない | `app.py:533-544` 採番にロック無し、`doc_number` に UNIQUE 制約無し、`year` は表示のみ | 中 | 中 | v2.5.3 | 未着手 |
| D-05 | DB接続が閉じられていない（20箇所） | `with get_db() as conn:` は sqlite3 ではコミットのみで close しない。`app.py:465,665,674,683,692,710,717,728,740,753,777,788,800,808,996,1189,1213,1229,1245,1254` | 中 | 小 | v2.5.3 | 未着手 |
| D-06 | 裸の `except: pass` で失敗が黙殺される | `app.py:95-96,163-164,764,836,969-970,1104-1118`。特に `:1104-1118` は `DROP TABLE day_customers` を含み、途中失敗でテーブル消失に気づけない | 中 | 小 | v2.5.3 | 未着手 |
| D-07 | 添付・名刺をBase64でDBに直接格納、サイズ上限なし | `app.py:174-184,689-706`。名刺一覧 `:672-679` は画像込みで全件返す | 中 | 大 | v2.6 | 未着手 |
| D-08 | バックアップに取引・カレンダー・ファイル・名刺・テンプレ・履歴が含まれない | `app.py:951-987` は customers/documents/company の3表のみ | 高 | 中 | v2.5.3 | 未着手 |
| D-09 | localStorage の設定（税率・端数・源泉・テーマ・レイアウト）がバックアップ対象外 | `index.html:5382-5383,5401,5470,5482,5024` / `js/pdf.js:963` | 中 | 中 | v2.5.3 | 未着手 |
| D-10 | 自動バックアップが存在しない（最終 2026-03-23） | V1撤去時に `com.nexutha.crm.backup.plist` を削除、V2で未再構築 | 高 | 中 | 別途（Part 6） | 未着手 |
| D-11 | 旧V2の実DBが72MBだが94%が空きページ。削除済みPIIが残留の可能性 | freelist 16,661ページ／実使用約4.5MB | 中 | 小 | 別途（Part 6） | 未着手 |

---

## 5. 税計算 / PDF（P）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| P-01 | 税計算が二重実装。片方は軽減8%・非課税を無視して全額10%課税 | 本則 `index.html:3545-3581` `calcDocTotal` に対し、別経路が10%固定 | 高 | 中 | v2.5.3 | 未着手 |
| P-02 | 税率 `undefined` の明細が消費税ゼロになる | `js/pdf.js:914-915` はどちらにも加算しない。一方 `js/pdf.js:35` は `?? 10`。**同一ファイル内で方針が逆** | 高 | 小 | v2.5.3 | 未着手 |
| P-03 | 代替PDF経路が別の金額を出す | `js/pdf.js:238-371` `generatePDF_print` は tax8/tax10 を無視（`:355-357`）、割引・源泉の行が無く、合計は割引前（`:345`）。html2canvas失敗時のみ動くため気づきにくい | 高 | 中 | v2.5.3 | 未着手 |
| P-04 | サーバーが税額を検証しない | `app.py:485,495,1026-1041` はクライアントの数字をそのまま保存 | 中 | 中 | v2.6 | 未着手 |
| P-05 | 請求書化・領収書化で11列が欠落（bank/payment_due/withholding/tax10/tax8/discount/staff/memo/validity/show_stamp/customer_name） | `index.html:3199-3244` の `receiptData` に含まれない。**正しい実装 `convert_document` は旧V2にあったが electron版では削除済み** | 高 | 中 | v2.5.3 | 未着手 |
| P-06 | PDFが画像貼付のため文字選択・検索・コピー不可 | `js/pdf.js:178-181` html2canvas + jsPDF | 中 | 大 | v2.6 | 未着手 |

---

## 6. フロントエンド構造（U）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| U-01 | 全ロジックが `index.html` の単一 `<script>`（1931-6734, 4,804行）に集中。1行の早期例外で全機能停止 | 同左。N-01の被害が全損になる根本原因 | 高 | 大 | v2.6 | 未着手 |
| U-02 | `js/*.js` が index.html のグローバルに全面依存（双方向依存）。読み込み順が変わると壊れる | `index.html:66-68` の順序固定。`handleError`/`showToast`/`API_BASE`/`NAPI`/`getRoundFn`/`calcDocTotal`/`layoutPositions`/`_previewDocData` など | 高 | 大 | v2.6 | 未着手 |
| U-03 | `js/pdf.js:375,632` が裸の `allDocuments` を参照。`index.html:2179` の宣言なし代入に依存 | 同左。**掃除コメント（`:2077`）の対象に見えるため削除されやすい。消すとPDF再発行とプレビューが壊れる**。`'use strict'` 付与でも壊れる | 高 | 小 | v2.5.3 | 未着手 |
| U-04 | `window.APP` への状態移行が未完（4/10キーのみ）。同じ意味の値が2箇所に存在 | `state.js:5-16` vs `index.html:2078-2083,3350`。`editingId`/`docType`/`detailCustomerId`/`wizard`/`currentIndustryFilter`/`editingDocId` が二重管理 | 中 | 中 | v2.6 | 未着手 |
| U-05 | `onclick` 属性に20以上の関数名が文字列で埋め込まれ、静的検査で追えない | `js/renderer.js:120,521,527,528,579,585,587,591-594,666,667,690,692,710,762,764,788,878-883` ほか | 中 | 大 | v2.6 | 未着手 |
| U-06 | モバイル下部ナビが未配線 | `showPage` が `[data-mpage]` を探すがHTMLに該当要素0件 | 低 | 小 | v2.6 | 未着手 |
| U-07 | デッドコード（`_rebuildPreviewOLD_UNUSED` 67行に旧税計算、`_deleteFromDetail_legacy` ほか計12関数） | `js/pdf.js:820-887` / `index.html:2801-2808` ほか | 低 | 小 | v2.5.3 | 未着手 |

---

## 7. ビルド / 配布（B）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| ~~B-01~~ | タグpushで即公開リリースが作られGOを挟めない | `release.yml` の `gh release create` に `--draft` が無かった | 公開ブロッカー | 小 | v2.5.2 | **対応済**（ba64b3b） |
| B-02 | **`ai-chat.html` と `node_modules` が配布物に入っていない → 配布版でAI機能が常に失敗** | `package.json:93-97` の `files` と `:59-91` の `extraResources` のどちらにも無い。`main.js:447` が読む先が存在しない。`index.html:5299` の web-llm import も404 | 高 | 中 | v2.5.3 | 未着手 |
| B-03 | ビルド前チェックがdistを除外しており、`dist/backend/data/` のDB混入を検出できない | `check-before-build.sh:34` の `grep -v "dist"` | 中 | 小 | v2.5.3 | 未着手 |
| B-04 | 開発版のPythonパスが個人環境固定 | `main.js:213` `/Users/runa.yasu/.pyenv/versions/3.10.6/bin/python3.10` | 中 | 小 | v2.5.3 | 未着手 |
| B-05 | Windowsビルド設定はあるが動作しない | `package.json:48-57` に nsis 定義。しかし `main.js:221-232` は `.exe` 非対応、`reclaimPort` は `lsof` 依存、`process.kill(-pid)` はWindows非対応、`app.py:38-46` のDBパスはmacOS専用分岐のみ | 中 | 大 | v2.6 | 未着手 |
| B-06 | Apple署名アイデンティティ・Team IDが直書き | `package.json:42` | 低 | 小 | v2.6 | 未着手 |

---

## 8. プロセス管理（M）

| ID | 症状 | 原因（ファイル:行） | 深刻度 | コスト | リリース | 状態 |
|---|---|---|---|---|---|---|
| M-01 | ポート9876を使う**他人のプロセスを名前確認せずSIGKILL**する | `main.js:169-187` `reclaimPort()`。コメントは「占有者は自分自身に限られる」と仮定しているが保証は無い | 中 | 小 | v2.5.3 | 未着手 |
| M-02 | バックエンド終了がSIGTERMのみでSIGKILLの追い打ちが無く、居座る可能性 | `main.js:190-203` | 中 | 小 | v2.5.3 | 未着手 |
| M-03 | ログ出力が実質ゼロ（Python側） | `app.py` に `logging` 0件、`print` 1箇所のみ。裸 `except` と組み合わさり障害が無音 | 中 | 小 | v2.5.3 | 未着手 |

---

## 9. 未完成の機能（F）

| ID | 症状 | 根拠 | 深刻度 | リリース | 状態 |
|---|---|---|---|---|---|
| F-01 | 「取引中心アーキテクチャ」がDB層のみ。取引API 7本が未使用、閲覧・編集UIが無い | 実DBで取引258件中256件が書類ゼロ、252件が存在しない顧客を参照 | 中 | v2.6 | 保留（方針判断待ち） |
| F-02 | 内税/外税の切替が未実装（文字列がリポジトリに0件） | 実装は非課税(0%)のみ＝常に外税 | 中 | v2.6 | 保留 |
| F-03 | 納品書が存在しない | 「納品」「delivery」0件 | 低 | v2.6 | 保留 |
| F-04 | E2Eテストが electron版に存在しない | `tests/` は旧V2のみ | 高 | v2.5.3 | 未着手（`docs/testing/E2E_CASES.md` に定義済み） |

---

## 付録: 対応済みの記録

| ID | 症状 | 対応 | 検証 |
|---|---|---|---|
| W-01 | 起動・更新後の白画面 | `c631337` 多重起動防止・ポート衛生・プロセスグループ終了・スプラッシュ追加 / `692bb1e` 再起動暴走防止 / `13f07e9` PyInstaller onefile→onedir（起動19秒→0.4秒） | 機械確認で `Resources/backend` がディレクトリ化していることを実測。実機検証で描画正常を確認（ライセンス画面まで到達） |
| B-01 | CIがタグpushで即公開 | `ba64b3b` `gh release create` に `--draft` 追加＋恒久ルールをコメント明記 | ドラフト作成を実測。未認証で `latest-mac.yml` が HTTP 404、`releases/latest` が v2.5.1 のままであることを確認 |

**未リリース**: 上記はすべて `main`(`ba64b3b`) にあるが、公開済みの最新は **v2.5.1**。v2.5.2 はドラフトのまま。
